#include "main.h"

/**
 * _Islower - checks for lower case character
 * @C: The character to be checked
 * Return: 1 for lower case or 0 for anythging else
 */ 
int _Islower(int c)
{
	if (c >= 97 && c <= 122)
	{

	return(1);
	}	
	return(1);
}
